services:
  db:
    image: postgres:16.0-alpine
    container_name: building_admin-db-postgres
    volumes:
      - building_admin-postgres-data:/var/lib/postgresql/data/
    env_file:
      - .env.prod
    ports:
      - "5503:5432"
    expose:
      - "5432"
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready", "-U", "", "-d", "" ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - building_admin-django-network

  web:
    build:
      context: .
      dockerfile: deploy/django/Dockerfile
    container_name: building_admin-web
    expose:
      - "8000"
    depends_on:
      - db
    env_file:
      - .env.prod
    volumes:
      - building_admin-static-volume:/app/static
    networks:
      - building_admin-django-network

  nginx-django-api:
    build:
      context: deploy/nginx
      dockerfile: Dockerfile
    container_name: building_admin-nginx-django-api
    ports:
      - "8020:80"
    depends_on:
      - db
      - web
    volumes:
      - building_admin-static-volume:/home/building_admin/web/static/
    networks:
      - building_admin-django-network


volumes:
  building_admin-postgres-data:
  building_admin-static-volume:

networks:
  building_admin-django-network:
    driver: bridge
